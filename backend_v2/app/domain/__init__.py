"""
Domain Layer — InterviewAce Clean Architecture.

This layer contains:
- entities/      Pure business objects (dataclasses), no framework deps
- interfaces/    Abstract base classes (ports) that infrastructure implements
- value_objects/ Enums, constants, and immutable value types
- services/      Domain services with pure business logic
- exceptions.py  Domain-specific exception hierarchy
"""
