# ── Backend API URL (server-side only — never exposed to browser) ──
# LOCAL:
BACKEND_URL=http://localhost:8000/api/v1
# PRODUCTION (Render): use your Render web service URL
# BACKEND_URL=https://interviewace-api.onrender.com/api/v1

# ── Public variables (prefixed with NEXT_PUBLIC_ — available in browser) ──
NEXT_PUBLIC_APP_NAME=InterviewAce
# LOCAL:
NEXT_PUBLIC_APP_URL=http://localhost:3000
# PRODUCTION (Vercel): set to your Vercel URL
# NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
